cmake_minimum_required(VERSION 2.8)
add_compile_options(-std=c++11)

project( generateMaterialScript )
add_executable( generateMaterialScript generateMaterialScript.cpp )

project( shelfArrangement )
add_executable( shelfArrangement shelfArrangement.cpp )


